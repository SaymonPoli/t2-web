version: "3.9"

services:
  couchdb:
    image: couchdb
    container_name: couchdb

    restart: always

    ports:
      - "127.0.0.1:5984:5984"

    environment:
      COUCHDB_USER: admin
      COUCHDB_PASSWORD: 123

    volumes:
      - couchdb_data:/opt/couchdb/data

volumes:
  couchdb_data:
